#ifndef COMMON_ENUMS
#define COMMON_ENUMS

namespace fr
{

namespace enums
{

enum class ApplicationMode
{
    REGISTRATION = 1,
    RECOGNITION = 2,
    UNKNOWN = 3
};

enum class Extensions
{
    JPG = 0,
    PNG = 1
};

} // namespace enums
} // namespace fr

#endif
